<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://etherio.github.io/cdn/normalize/normalize.css">
    <link rel="stylesheet" href="https://etherio.github.io/cdn/milligram/milligram.css">
</head>

<body>
    <div class="container">
        <form action="/users/login" method="POST">
            <fieldset>
                <h2>Log In</h2>

                <label for="username">Username</label>
                <input type="text" name="username" id="username" placeholder="Username" autocapitalize="off" value="<%= typeof username !== 'undefined' ? username : '' %>">

                <label for="password">Password</label>
                <input type="password" name="password" id="password" placeholder="Password">

                <button type="submit">Log In</button>
                <a class="button button-outline" href="/users/register">Create an account?</a>
            </fieldset>
        </form>

        <% if (typeof errors !== 'undefined') {%>
            <ul>
                <% errors.map(error => { %>
                    <li>
                        <%= error %>
                    </li>
                    <% }) %>
            </ul>
            <% } %>

    </div>
</body>

</html>